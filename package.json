{
  "name": "walfo",
  "version": "0.1.0",
  "license": "ISC",
  "scripts": {
    "install": "yarn install --ignore-scripts && yarn lerna bootstrap",
    "test": "lerna run --stream test",
    "start": "./start.sh"
  },
  "dependencies": {
    "lerna": "^3.13.1"
  }
}
